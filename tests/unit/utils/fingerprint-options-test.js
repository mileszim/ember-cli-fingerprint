import fingerprintOptions from 'dummy/utils/fingerprint-options';
import { module, test } from 'qunit';

module('Unit | Utility | fingerprint-options', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = fingerprintOptions();
    assert.ok(result);
  });
});
